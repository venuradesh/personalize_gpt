import { PgptTranslatePipe } from './pgpt-translate.pipe';

describe('PgptTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new PgptTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
